#include <stdio.h>

int main (){
    int note = 36;
    int velocity = 89;
    int channel = 6;
printf("This MIDI note consists of:\n\tPitch: %i\n\tVelocity: %i\n\tChannel: %i", note, velocity, channel);

}